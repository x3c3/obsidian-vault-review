{
  "name": "vault-review",
  "version": "1.0.2",
  "description": "Randomly review your vault and track progress",
  "type": "module",
  "scripts": {
    "dev": "bun run build.ts --watch",
    "build": "bun run check && bun run build.ts",
    "check": "bun run typecheck && biome check .",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "validate": "bun run scripts/validate-plugin.ts",
    "version": "bun run version-bump.ts",
    "test": "bun test",
    "deploy": "cp main.js manifest.json ~/source/philoserf/notes/.obsidian/plugins/vault-review/"
  },
  "author": "Mark Ayers",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^2.4.5",
    "@types/bun": "^1.3.9",
    "@types/node": "^25.3.3",
    "obsidian": "^1.12.3",
    "typescript": "^5.9.3"
  }
}
